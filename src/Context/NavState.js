import React, { Children } from 'react'

const NavState = (props) => {
// let Signup = Signup ;


  return (
    <Network.Provider>
      {Children.props}
    </Network.Provider>
  )
}

export default NavState
